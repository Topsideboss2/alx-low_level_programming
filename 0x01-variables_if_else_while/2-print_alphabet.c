#include <stdio.h>
#include <string.h>
/**
 * main - Execute
 *
 * Return: 0 (Success)
 */
int main(void)
{
	int alf;

	for (alf = 'a'; alf <= 'z'; alf++)
		putchar(alf);
		putchar('\n');

	return (0);
}
